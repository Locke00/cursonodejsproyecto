//alert('hoal')
//let cookieGuardada = { modo: req.cookies.token }
//console.log(document.cookie);


console.log(document.cookie);
/*
function obtenerValorDeCookie(nombre) {
  const cookies = document.cookie.split(';');
  for (const cookie of cookies) {
      const [cookieName, cookieValue] = cookie.trim().split('=');
      if (cookieName === nombre) {
          return decodeURIComponent(cookieValue);  // Decodificar el valor de la cookie
      }
  }
  return null;
}

const valorDeRol = obtenerValorDeCookie('rol');

if (valorDeRol) {
    console.log(`El valor de la cookie "rol" es: ${valorDeRol}`);
} else {
    console.log('La cookie "rol" no está presente o su valor es nulo.');
}*/